# Server-only values. Do not prefix secrets with NEXT_PUBLIC_.
BACKEND_URL=http://127.0.0.1:8000
API_KEY=
# Open this exact origin. It must match the optional Microsoft mailbox callback origin.
DASHBOARD_ORIGIN=http://localhost:3000
# Set true behind HTTPS. HTTPS origins also enforce Secure cookies automatically.
COOKIE_SECURE=false
